# Copy to android/keystore.properties (gitignored) for release signing.
# Keep the .jks file OUTSIDE the repo (e.g. ~/fromvictory-android-keys/).

storeFile=/absolute/path/to/fromvictory-release.jks
storePassword=REPLACE_ME
keyAlias=fromvictory
keyPassword=REPLACE_ME
