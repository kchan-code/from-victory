# macOS
.DS_Store
Thumbs.db

# Node
node_modules/

# Next.js
.next/
out/
build/

# Vercel
.vercel

# Environment variables
.env
.env.local
.env*.local
!.env.example

# IDE
.vscode/
.idea/

# Logs
*.log
npm-debug.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Supabase
supabase/.branches
supabase/.temp

# CI artifacts (kids-privacy-officer staging dir — diff + review output)
.pr-review/

# Playwright
apps/web/.env.test
apps/web/e2e/.auth/
apps/web/playwright-report/
apps/web/test-results/

# Vitest coverage (FV-380)
apps/web/coverage/

# ElevenLabs voice bake-off candidates (FV-285) — rendered locally for
# by-ear A/B, never committed. The README.md the tool writes alongside them
# IS committable (not excluded by this pattern).
**/docs/audio-ab-*/**/*.mp3

# Native / Capacitor — never commit signing material or local SDK paths
*.jks
*.keystore
*.mobileprovision
*.p12
AuthKey*.p8
apps/native/android/local.properties
apps/native/android/keystore.properties
apps/native/android/app/build/
apps/native/android/build/
apps/native/ios/App/Pods/
apps/native/ios/DerivedData/
# Untracked local marker of unknown provenance (contents 808e3331, mtime
# 2026-06-22) — excluded from the Codex mirror commit until its producer is
# identified (FV-432 / KC decision 2026-07-20). Do not regenerate or interpret.
/.hash
