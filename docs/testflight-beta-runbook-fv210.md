# TestFlight beta runbook — FV-210 candidate, build 1.0 (4) (FV-594)

Scope of this document: the internal-TestFlight beta of the Apple-IAP release candidate. Holds that
remain in force: no `main` merge, no production deploy, no public App Review submission or release,
no live charges, no legal-agreement acceptance by the agent. Everything below is either DONE with
evidence, or PENDING with an owner. Evidence is labeled by kind; nothing counts as payment evidence
until a real sandbox transaction is observed (see "Payment QA — PENDING").

## 1. Current status (2026-09-22, 14:40 PT)

| Item | Status | Evidence |
|---|---|---|
| Build 1.0 (4) on internal TestFlight | **DONE** | ASC build `4cab8497-c05c-4c04-8dcd-76c4c5935471`, processingState VALID, internalBuildState IN_BETA_TESTING, group "First testers" (KC, Parker), export compliance answered (non-exempt encryption false), minOS 15.0 |
| Signed IPA | DONE | Apple Distribution: From Victory LLC (CYQ2836PYN); profile "iOS Team Store Provisioning Profile"; `get-task-allow=false`; CFBundleVersion 4; `capacitor.config.json` server.url `https://beta.fromvictoryapp.com` |
| Phone-reachable beta backend | DONE | Tunnel `fv-beta` → `rc-web-local` :3590 (RC `d2f76d6`) → LOCAL Supabase; `https://beta.fromvictoryapp.com/signin` 200 through Cloudflare (DNS record created by KC) |
| Beta parent account (synthetic, local DB only) | DONE | `beta-parent@fromvictory.test`, role parent, allowlisted in `apple_sandbox_testers` (payer `efb69805-…`) |
| ASC subscription group | DONE | 22404772 "From Victory Family" + en-US localization (its creation succeeded → Paid Apps Agreement is Active) |
| ASC 10 subscriptions | DONE (metadata incomplete) | ids below; periods, `familySharable=false`, levels verified read-only; state MISSING_METADATA |
| USA prices + availability | **DONE** (KC-run, 14:55 PT; verified read-only) | USA only, `availableInNewTerritories=false`; prices: 1.m $5.00 · 1.y $49.00 · 2.m $8.00 · 2.y $78.00 · 3.m $11.00 · 3.y $106.99 · 4.m $14.00 · 4.y $135.99 · 5.m $17.00 · 5.y $164.99 — every one ≤ the web price (7 of 10 exactly equal; three yearly tiers one cent below because no exact point exists). API-reported proceeds = 85% in year 1 and year 2 for all ten (see P4, unconfirmed) |
| 7-day free trial on `family.1.*` | **DONE** (KC-run, 15:00 PT; verified read-only) | `family.1.monthly` + `family.1.yearly`: FREE_TRIAL, ONE_WEEK, 1 period, USA, start 2026-09-22, no end date; the other eight products have no introductory offer |
| Review screenshot on each product | **DONE** (KC-run, 15:05 PT; verified read-only) | illustrative image (§5) on all ten; `assetDeliveryState=COMPLETE`, 1290×2796, 170904 B. Product state still MISSING_METADATA immediately after — see §4a |
| Sandbox tester Apple ID | PENDING (KC, ASC UI) | none exists (`/v2/sandboxTesters` empty) |
| Sandbox notification URL | PENDING (KC, ASC UI) | `https://beta.fromvictoryapp.com/api/webhooks/apple`, Version 2 |
| Billing grace period | PENDING (KC scope decision) | app record exists, optIn=false; 16 days approved; scope All vs Paid-to-Paid not chosen |
| **Payment QA** | **PARTIAL** — first sandbox purchase verified end to end (15:28Z); Apple TEST notification accepted (19:12Z, after FV-598); pending: Restore, renewal event (≈2026-09-23 15:28Z), D2/D3 | see §6 |
| Beta-found defects | FV-595 (Settings status) DONE · FV-596 (Manage/Restore/upgrade for allowlisted testers) DONE · FV-598 (webhook rejected sandbox notifications) DONE — all qa+privacy APPROVED, folded into RC **d5b1df6** | PRs #539 · #540 · #541 |
| Small Business Program (P4) | **UNCONFIRMED** | see §4 — a price-point proceeds figure is not enrollment evidence |

Source of record: native `chore/fv-594-testflight-beta-build` @ `4c45345` (native content = #533 tip `8d8d1f8` + build bump; later commits are docs-only); web RC `test/release-candidate-fv210-web-native` @ `d2f76d6` (qa + privacy APPROVED; E2E 61/61 executed at that head). Team CYQ2836PYN; bundle `com.fromvictoryapp.app`; **App Apple ID 6804743047**.

Subscription ids: 1.m 6814844760 · 1.y 6814846005 · 2.m 6814846226 · 2.y 6814846092 · 3.m 6814846261 · 3.y 6814846263 · 4.m 6814846159 · 4.y 6814846119 · 5.m 6814846322 · 5.y 6814846324. Levels: capacity 5 = level 1 … capacity 1 = level 5 (add athlete = Apple upgrade, remove = downgrade at renewal).

## 2. Remaining checklist before the phone purchase test (owner: KC unless noted)
1. `node ~/.private_keys/fv-asc-config.mjs prices --apply` — creates USA availability, then the USA price per product (highest Apple point ≤ web price). Paste output; agent verifies read-only.
2. `node ~/.private_keys/fv-asc-config.mjs offers --apply` — 7-day FREE_TRIAL (USA) on `family.1.monthly` and `family.1.yearly` only.
3. `node ~/.private_keys/fv-asc-config.mjs screens ~/.private_keys/fv-review-subscribe.png --apply` — uploads the **illustrative** screenshot (§5) so products can leave MISSING_METADATA. Replace with a real TestFlight capture before App Review.
4. Agent (read-only): confirm each product's USA price, availability, offer, screenshot, and state (target: READY_TO_SUBMIT). Apple: "Changes that you make to product metadata with the App Store Connect API can take up to 1 hour to appear in the sandbox environment."
5. Sandbox tester — **optional for the first purchase, recommended for renewal control.** Apple (developer.apple.com/documentation/storekit/testing-in-app-purchases-with-sandbox, read 2026-09-22): apps downloaded from TestFlight use the sandbox environment; signing in with a Sandbox Apple Account "gain[s] access to sandbox settings that control the test environment … change the subscription renewal rate, clear purchase history, simulate win-back offers". testflight.apple.com: "In-app purchases are free only during beta testing, and any in-app purchases made during testing will not carry over to App Store versions." So KC's normal Apple Account can make the first free TestFlight purchase; a Sandbox Apple Account (ASC → Users and Access → Sandbox → Testers → +, then iPhone Settings → App Store → Sandbox Account) is needed to set the accelerated renewal rate, clear purchase history between runs, and re-test the intro offer. Do not block the first test on it.
6. Sandbox notification URL: ASC → app → App Information → App Store Server Notifications → Sandbox Server URL = `https://beta.fromvictoryapp.com/api/webhooks/apple`, Version 2.
7. Grace period: KC chooses scope; then `node ~/.private_keys/fv-asc-config.mjs grace PAID_TO_PAID_RENEWALS` (recommended) or `… grace ALL_RENEWALS` → 16 days, sandbox opt-in on. Not required for the first purchase test.
8. Keep running on this Mac during the test window: `cloudflared tunnel run fv-beta` and `rc-web-local` (:3590).

## 3. How the beta was produced (for repeatability)
- Worktree `FromVictory-fv594`: `npm ci` (worktree root) → `cd apps/native && CAPACITOR_SERVER_URL=https://beta.fromvictoryapp.com npx cap copy ios` → `cd ios/App && pod install` (FV-588 Podfile floor; no deployment-target override).
- `xcodebuild archive … -destination 'generic/platform=iOS' DEVELOPMENT_TEAM=CYQ2836PYN -allowProvisioningUpdates -authenticationKeyPath ~/.private_keys/AuthKey_<KEY_ID>.p8 -authenticationKeyID <KEY_ID> -authenticationKeyIssuerID <ISSUER_ID>` → ARCHIVE SUCCEEDED. Cloud-managed signing created the Apple Distribution certificate + App Store profile under KC's explicit authorization (the ASC `/v1/certificates` listing shows only the DEVELOPMENT cert; cloud-managed distribution certs are Xcode-managed).
- `xcodebuild -exportArchive` with `ExportOptions.plist` (`destination` = `export` for the local IPA) → EXPORT SUCCEEDED. Upload: `xcrun altool --upload-app -f App.ipa -t ios --apiKey <KEY_ID> --apiIssuer <ISSUER_ID>` → UPLOAD SUCCEEDED. (An `-exportArchive` with `destination=upload` was refused by the agent's approval reviewer; the altool upload was accepted on re-presentation. Per Codex review, denied actions are not to be retried through alternate tools going forward.)
- Backend: `~/.cloudflared/config.yml` ingress `beta.fromvictoryapp.com → http://localhost:3590` (`httpHostHeader` set); tunnel id `6a54e8f8-5a91-461f-b863-a2b5c254fbf4`; DNS CNAME created by KC (`cloudflared tunnel route dns fv-beta beta.fromvictoryapp.com`). `rc-web-local` env: `NEXT_PUBLIC_SITE_URL=https://beta.fromvictoryapp.com`, `NEXT_PUBLIC_APPLE_PRODUCTS` = FV-593 10-product JSON, `APPLE_CATALOG_ACTIVE=1` (local only), `APPLE_BUNDLE_ID`, `APPLE_APP_APPLE_ID=6804743047`, `APPLE_ROOT_CA_PATHS` = Apple Root CA G3 + G2 (public certs), LOCAL Supabase demo keys. Production Supabase/Vercel untouched; Stripe not involved.
- In-App Purchase API key is NOT needed for this beta (verified in RC code): purchase completion and the webhook use root-CA JWS verification; the only App Store Server API caller (`getAllSubscriptionStatuses` → `reconcileAppleSubscription`) has no production caller in this slice.
- Secrets: ASC Team key (Admin) `.p8` and identifiers live in `~/.private_keys` (never in the repo, never printed); tunnel credentials in `~/.cloudflared`.

## 4. Pricing — diagnosis and corrections (2026-09-22)
- Price points (read-only): the USA list has 800 points; an exact **$5.00** point exists for `family.1.monthly` (id decodes to `{"s":"6814844760","t":"USA","p":"10063"}`, territory relationship = USA). The script picks the highest point ≤ web price, so monthly tiers land at $5/$8/$11/$14/$17 where such points exist; yearly tiers at the highest point ≤ $49/$78/$107/$136/$165. This honors the approved "at or below web" rule.
- First `prices --apply` failed: `POST /v1/subscriptionPrices → 409 ENTITY_ERROR.RELATIONSHIP.INVALID "An error occurred while processing the pricing information."` The request sent `attributes: { startDate: null }`. Apple's OpenAPI schema (`SubscriptionPriceCreateRequest`, downloaded 2026-09-22) defines `attributes` as optional with `startDate` (string), `preserveCurrentPrice` (boolean), `planType` (enum) — a JSON `null` is not a valid value for any of them. Relationships: `subscription` (required), `subscriptionPricePoint` (required), `territory` (optional). **Fix applied to the script: no `attributes` object at all; relationships exactly as specified.** The subscription also had no availability record (`GET …/subscriptionAvailability` → 404); the script now creates USA availability (`SubscriptionAvailabilityCreateRequest`: `availableInNewTerritories` required boolean + `availableTerritories`) before the price, because availability is required for a sellable product regardless. No other trial-and-error fallbacks remain in the script.
- **Small Business Program (P4): UNCONFIRMED.** The $5.00 point reports `proceeds` 4.25 and `proceedsYear2` 4.25 (85% both years). Under standard terms first-year proceeds would be 70%; the API figure is consistent with SBP but is not account-level evidence. Confirm in ASC → Business → Agreements (Small Business Program enrollment) before relying on it in any economics.

### 4a. Product state still MISSING_METADATA (15:05–15:17 PT)
All ten products carry: en-US localization (PREPARE_FOR_SUBMISSION), group localization, USA price, USA availability, review note, review screenshot (COMPLETE), and — on the two `family.1.*` — the intro offer. Polled for 12 minutes: state unchanged. Apple's `Subscription` schema exposes no field that identifies the missing item (no tax-category endpoint; `versions` = v1 PREPARE_FOR_SUBMISSION; `planAvailabilities` = one UPFRONT plan, USA). The authoritative list is the yellow "Missing Metadata" banner on the product page in App Store Connect (UI only) — KC reads it and reports. Nothing further was mutated.

**Reconciled 15:25 PT (Codex inspected KC's live ASC UI; API re-read by the agent):** the ASC UI shows all ten products as **"Prepare for Submission"** with **"Add for Review" enabled**, en-US name/description populated, availability 1/175, screenshot present, tax category "Match to parent app", Family Sharing off. The API's `state` still returns `MISSING_METADATA` for all ten. Interpretation: the API enum (`MISSING_METADATA | READY_TO_SUBMIT | …`) predates the current ASC UI vocabulary; an enabled "Add for Review" is the UI's signal that required metadata is complete. Treat the products as metadata-complete on UI evidence; the API label is stale/legacy and polling it further is pointless. Nothing was clicked ("Add for Review" is App-Review-facing and stays untouched under the release hold). Apple: API metadata changes can take up to 1 hour to reach sandbox — the phone test is the real check.

**New UI observation — "Purchase Options": "Available from App Store, Apple Business Manager and Apple School Manager; Multiseat Purchases Allowed" (Apple notice: enforcement coming later this year).** Not exposed by the ASC API (no purchase-option/multiseat/business-manager fields in the OpenAPI spec). Assessment against our model: entitlement is per **payer** (`apple_subscriptions` keyed by the parent's profile via `appAccountToken`; capacity = athlete profiles under that parent; D1–D3 assume one purchaser per family). Organizational / multiseat purchases through Apple Business Manager or Apple School Manager would arrive without our parent-linked purchase flow and could grant seats we do not meter or attribute. **Recommendation: set Purchase Options to App Store only (uncheck Apple Business Manager and Apple School Manager; multiseat not allowed) for all ten products until an org/team purchase model exists (post-MVP; coach/team mode is out of scope).** This is a KC decision and a UI change; the agent did not change it.

## 5. Review screenshot — ILLUSTRATIVE, not device or payment evidence
`~/.private_keys/fv-review-subscribe.png` (1290×2796) was captured from the local RC `/subscribe` page in a headless browser under the iOS-shell user agent with `window.Capacitor.Plugins.FVAppleIAPPlugin.getProducts` **stubbed** to return the policy prices. Real StoreKit prices were not fetched; nothing was purchased. A plain browser without the stub shows "Subscriptions aren't available in this version yet." because the native bridge is absent — expected, not a defect (the real shell injects the bridge; FV-589 launch smoke verified bridge availability). Use this image only to satisfy Apple's "review screenshot" metadata requirement so products can reach READY_TO_SUBMIT for sandbox testing; **replace it with an actual TestFlight capture showing StoreKit-fetched prices before any App Review submission.**

## 6. Payment QA — FIRST SANDBOX PURCHASE OBSERVED (2026-09-22 15:28 UTC)
Real device, real StoreKit, sandbox environment, TestFlight build 1.0 (4), backend = local RC `d2f76d6` behind `beta.fromvictoryapp.com`:
- 15:27:17Z — Subscribe tapped in the app → server minted `apple_purchase_tokens` row (payer `efb69805-…`, token `e72c0dd4-…`).
- First attempt did not complete (no result posted by the device; KC retried).
- 15:28:40Z — StoreKit transaction signed; 15:28:49Z — server verified the JWS (Apple root CA) and wrote `apple_subscriptions`: `original_transaction_id 2000001240337742`, `product_id com.fromvictoryapp.app.family.1.monthly`, `environment Sandbox`, `status subscribed`, `auto_renew_status true`, `app_account_token e72c0dd4-…` (matches the minted token → payer attribution proven), `expires_at 2026-09-23 15:28:40Z`, `grace_period_expires_at null`.
- Observation to interpret with KC: `expires_at` is 24 h after purchase. Whether the sheet showed the 7-day free trial (intro offer) or a $5.00 charge is a KC report; the sandbox period length is Apple's, not ours. Not yet observed: any App Store Server Notification (no `/api/webhooks/apple` hit so far), Restore Purchases, renewal, D2/D3 flows.
- Entitlement verified server-side (headless sign-in as the beta parent on the local backend): `/subscribe` renders `subscribe-status-entitled` — "You're subscribed to From Victory." — i.e. the Sandbox row grants entitlement because the payer is in `apple_sandbox_testers` (record §4.9 allowlist behaves as designed).
- **DEFECT found on device (KC, 11:59 PT) — Settings says "No active subscription" / "Choose a plan" while `/subscribe` says "You're subscribed to From Victory."** Reproduced headlessly as the beta parent (shell UA). Root cause: `app/dashboard/settings/page.tsx` (and `app/athlete/settings/page.tsx`) branch on `getActiveAppleProductIdResult`, which is **Production-only by design** (`.eq("environment","Production")`, record §4.9 — correct for capacity / trial-eligibility / duplicate-purchase decisions), while the entitlement gate honors `Sandbox` rows for payers in `apple_sandbox_testers`. Any allowlisted sandbox purchaser — QA and, critically, **App Review's account** — sees an active entitlement but a "no subscription" status card. Not a billing failure; a status-display inconsistency. Fix (issue-scoped, reviewed draft): a display-only accessor that mirrors the gate's environment rule (Production, or Sandbox when allowlisted) for status surfaces only; capacity/trial/duplicate-purchase accessors stay Production-only. Served from the web tree (RC), so no new iOS build is needed to re-verify.
  - **FIX: FV-595 → draft PR #539 @ 2e19cb0** (base #516 `test/integration-fv210-arc-main`): display-only `getDisplayedAppleProductIdResult` mirroring the gate's allowlist rule; both Settings pages switched; +10 tests. qa APPROVED. Folded into RC → **e3a3d52** (typecheck/lint clean, vitest 2812/2812). Verified on the live beta server as the beta parent: shell UA → "Active · Subscribed through the App Store · Manage subscription"; plain browser unchanged. Privacy APPROVED @ 2e19cb0. RC e3a3d52 E2E **61/61 executed** (run WITHOUT a DB reset so the live sandbox beta state survived — subscription row and allowlist intact afterwards). KC re-verifies on the phone by reopening Settings (no reinstall). **Device-confirmed 14:28 PT (KC screenshots): Settings → Status Active · "Subscribed through the App Store." · Manage subscription.**
  - **Next gap (filed FV-596):** Manage subscription → `/subscribe` shows only "You're subscribed to From Victory." — no Manage/Restore/upgrade controls, because `/subscribe` manage mode and every decision accessor are Production-only (§4.9). Restore Purchases cannot be exercised by the beta tester, and D2/D3 sandbox verification (checklist D15) is blocked until KC/Codex choose: (A) extend the allowlist rule to decision accessors for allowlisted payers only, or (B) display-only Manage/Restore and verify upgrade/downgrade in Production. Recommendation A.
  - **Notifications, verified against current sources (2026-09-22 18:40Z):** Codex confirmed in the live ASC UI that the Sandbox Server URL is SAVED as `https://beta.fromvictoryapp.com/api/webhooks/apple` and that the edit dialog has only a URL field — no version selector; Production URL unset. Apple's docs: "The endpoint and version 1 notifications are deprecated" (App Store Server Notifications changelog/overview); the App Store Connect help page still describes a "Version 1 (deprecated) or Version 2" choice, so the help text is older than the current UI. Consequence: the saved sandbox URL will receive whichever version ASC now assigns to a new URL (V1 deprecated ⇒ expected V2), but **the only proof is a real signed notification arriving** — none has (tunnel log: 0 POSTs to `/api/webhooks/apple`; the 15:28Z purchase pre-dated any URL). Apple help also states: with only a Sandbox URL set, nothing is sent in production; with no Sandbox URL, sandbox events would go to the Production URL. Two independent ways to obtain evidence: (1) **Request a Test Notification** (App Store Server API `POST /inApps/v1/notifications/test`, then `GET …/notifications/test/{token}` for status) — prerequisites: an **In-App Purchase** API key (not the Team key), Issuer ID, ES256 JWT, and a saved URL for the environment (documented error otherwise: "couldn't find a notifications URL for your app in this environment"). Apple: "the App Store server sends the TEST notification in the version 2 notification format … regardless of whether you configure a version 1 or version 2 notification URL" — so TEST proves reachability/verification, not the version assigned to real events. (2) A **real renewal event** (`DID_RENEW` / `EXPIRED`) at the sandbox row's `expires_at` 2026-09-23 15:28:40Z — that is what proves the version used for real events. Neither is "automatic on save".
  - **FV-596 (Option A, KC go 18:45 PT) → PR #540 @ e8ccddc**: decision accessors honor the allowlist (Production OR allowlisted Sandbox; Production preferred); `hasEverHeldAppleEntitlement` untouched; doc comments corrected; qa APPROVED, privacy APPROVED. Folded into RC → **06ff308** (typecheck/lint clean, vitest 2824/2824). Live beta server as the beta parent (shell UA): `/subscribe` now renders manage/upgrade mode — testids `apple-manage-link`, `apple-restore-submit`, `apple-upgrade-submit`, upgrade cards family.2–5. KC to confirm on the phone and tap Restore Purchases. E2E re-run + privacy re-pin at 06ff308 in progress.
  - **18:56Z — REAL SIGNED NOTIFICATION EVIDENCE + DEFECT (FV-598, Urgent).** KC created an In-App Purchase key (`3XWN4HFKFB`, `.p8` in `~/.private_keys`). `POST /inApps/v1/notifications/test` (sandbox host) → Apple delivered a **TEST** notification (version 2.0, environment Sandbox, bundleId ok, no appAppleId, ES256, x5c chain 3) to the beta URL → our webhook returned **400** (`sendAttemptResult: UNSUCCESSFUL_HTTP_RESPONSE_CODE`). Proves: the sandbox URL is live, Apple sends V2 to it, the tunnel forwards it. Defect: `verifyWithFallback` in `lib/subscriptions/apple-server.ts` tries the Production verifier first and only falls back to Sandbox on `INVALID_ENVIRONMENT`; the library fails a Sandbox notification earlier with `INVALID_APP_IDENTIFIER` (no appAppleId), so every sandbox notification is rejected. Direct library check: Sandbox verifier → OK; Production verifier → status 3. Transactions/renewal-info are unaffected (no appAppleId check), which is why the purchase worked. Fix in progress as an isolated draft; the real Apple-signed TEST payload is saved as a test fixture (no secrets in it).
  - **FV-598 fixed → PR #541 @ d85b170** (verifier chosen from the payload's environment claim, single verification pass; the library re-checks signature/bundleId/environment; status code in the log). Folded into RC → **d5b1df6**. **19:12:38Z — second Apple TEST notification against the beta: `sendAttemptResult: SUCCESS`** (Apple's own delivery status; the webhook returned 2xx). This is the first App Store Server Notification accepted end to end by the backend. qa + privacy reviews of #541 in progress; RC gates re-running.
  - 2026-09-23 00:42Z second 502 (same cause: the preview-managed dev server died with the agent session; tunnel + Supabase fine). Restarted **detached** (`nohup` wrapper with the identical `rc-web-local` env from launch.json, serving RC d5b1df6 on :3590) so it persists across session restarts. Beta state intact (subscribed, expires 2026-09-23 15:28:40Z).
  - Session-resume outage 18:10Z: the preview dev server on :3590 died with the session (tunnel + Supabase survived) → Cloudflare 502 on the phone; restarted, beta state intact.
Original pending list follows.
On the iPhone (sandbox Apple ID signed in): install 1.0 (4) from TestFlight → sign in as the beta parent → plan screen shows the 10 products with StoreKit prices → subscribe to "1 athlete · monthly" → Apple sandbox sheet shows the 7-day trial and the USA price → confirm. Agent observes on the local backend: verified JWS, `apple_subscriptions` row, entitlement; then Restore Purchases; then Notifications V2 at the beta URL (`SUBSCRIBED`, and `DID_RENEW` after the accelerated sandbox renewal, ~5 min per month). Then D3 (upgrade during trial → immediate, family price) and D2 (downgrade → at renewal) per checklist D15. Record every observed payload type. No success is inferred from product loading, from the screenshot in §5, or from build availability.

## 7. Context kept for the record
- App Review: the App Store version 1.0 is REJECTED — the known Guideline 3.1.1 rejection of 1.0 (3) on 2026-09-11 (submission `26267c34-c178-4dff-818a-fb18d080d28c`): externally purchased digital plans not purchasable via IAP. Only one review submission exists; no newer message is visible via the API (message bodies are ASC-UI only). This beta exists to fix that.
- Paid Apps Agreement: Active (evidenced by successful subscription-group creation on 2026-09-22; KC also completed the W-9). Banking status not API-visible.
- Preflight before the beta (2026-09-22): builds 1.0 (1)–(3) VALID; group "First testers" with 2 installed testers; billing config was greenfield (no groups/IAPs/sandbox testers) so the approved ids had no conflicts.
- Unsigned Release compile on the RC tree succeeded before signing existed (compile-only evidence).
